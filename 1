version: '3'

services:
  app:
    image: marina1327/app1-go:TAG=$(curl -s https://registry.hub.docker.com/v2/repositories/marina1327/app1-go/tags/ | jq -r '.results | sort_by(.last_pushed) | .[-1].name')
    env_file: .env
    #ports:
    # - PORT='${APP_PORT}':8080
    environment:
      - PORT=$APP_PORT
    command: /app/main

